# 阶段 5：AI、搜索、启动包与 Liquid Glass

最后更新：2026-07-21（Asia/Shanghai）

## 状态

- 🟢 已完成
- 分支：`codex/perf-bundle-and-glass`
- 回退基线：`main` / `1323462`（PR #8）
- 合并结果：PR #9 / `4b6dbe9`

## 已确认基线

| 项目 | 阶段 5 修改前 |
|---|---:|
| Vite 主 JavaScript | 491,243 B（gzip 150.86 kB） |
| Vite CSS | 71,982 B（gzip 14.81 kB） |
| Inter 字体资产 | 237,512 B，包含 Latin Extended、Cyrillic、Greek、Vietnamese 等未使用子集 |
| 1,200 段搜索平均耗时 | 0.2681 ms |
| 1,200 段详情 SSR 平均耗时 | 12.0131 ms |
| 10,000 媒体索引派生平均耗时 | 4.2217 ms |

## 基线问题

- [x] Markdown 解析器与 GFM、sanitize 依赖进入首包。
- [x] 流式 assistant 消息每次增量均重新执行完整 Markdown 解析。
- [x] AI 对话无“离开底部后停止自动滚动”判断。
- [x] 长对话一次挂载全部消息。
- [x] 设置、媒体库和 AI 对话面板均由入口静态加载。
- [x] 每次搜索都会重新对所有段落执行小写规范化。
- [x] 搜索结果列表会一次挂载全部匹配项。
- [x] 完整 Inter Variable CSS 输出七个字体子集；中文仍回退系统字体。
- [x] 多个嵌套面板重复使用 `backdrop-filter`，拖动 AI 分隔条时未降低玻璃绘制成本。

## 执行清单

### 5A AI 流式渲染与长会话

- [x] 流式消息使用安全纯文本预览，完成后才解析 Markdown。
- [x] Markdown 渲染器动态加载，插件与组件配置使用稳定模块常量。
- [x] 用户离开对话底部后停止自动滚动。
- [x] 默认只挂载最近 40 条消息，旧消息按需展开。
- [x] 增加流式、滚动和消息窗口回归测试。
- [x] 转写上下文按 `transcriptId + revision` 使用 8 项 LRU 缓存；旧记录无 revision 时保持无缓存，避免错误复用。

### 5B 转写搜索

- [x] 建立可复用的小写搜索索引。
- [x] 单段编辑时只更新对应索引项。
- [x] 搜索结果预览最多挂载 100 项，同时保留总数和上一项/下一项导航。
- [x] 1,200 个匹配项基准已覆盖大于 1,000 项场景。

### 5C 启动包与字体

- [x] 动态加载 AI 对话、设置、媒体库、转写详情和智能速览低频界面。
- [x] 为异步界面提供稳定占位状态。
- [x] 移除未使用的 Inter 字体子集，改用 Segoe UI Variable / Microsoft YaHei UI 系统回退。
- [x] 记录修改前后首包与字体资产差异。

### 5D Liquid Glass 绘制成本

- [x] 仅顶层容器保留实时背景模糊，上传、队列、状态和表头等子表面改用渐变、高光和阴影。
- [x] AI 消息、转写详情和媒体库滚动区域补充布局/绘制隔离。
- [x] AI 面板拖动期间暂时关闭顶层模糊并降低阴影成本。
- [x] 保持减少透明度、减少动态效果和高对比度降级。

### 5E 验证与版本管理

- [x] 本地 115/115 测试、构建、生产依赖审计和性能基准通过。
- [x] 应用内浏览器完成 1280×720 与 1080×700 验收；无横向溢出、错误遮罩或控制台 warning/error。
- [x] 更新总计划和续作日志。
- [x] 实现提交 `28701cd` 已推送，草稿 PR #9 已创建；首轮 Windows `test-and-build` 通过（1m45s）。
- [x] 最终 Windows `test-and-build` 通过（1m42s），PR #9 已 squash merge 为 `4b6dbe9`。

## 修改后结果

| 项目 | 修改前 | 修改后 | 变化 |
|---|---:|---:|---:|
| Vite 首屏主 JavaScript | 491,243 B | 240,874 B | -50.97% |
| 首屏主 JavaScript gzip | 150.86 kB | 76.96 kB | -48.99% |
| Inter 字体资产 | 237,512 B | 0 B | -100% |
| CSS | 71,982 B | 70,908 B | -1.49% |
| 1,200 段搜索平均耗时 | 0.2827 ms（同轮旧路径） | 0.1422 ms | 1.99× 吞吐 |

动态块：Markdown 163,064 B、设置 22,997 B、转写详情 19,922 B、媒体库 14,012 B、AI 面板 9,552 B，仅在对应功能首次打开时加载。

## 界面验收

- 1280×720：原文约 642px、AI 410px；Markdown 标题、列表、表格和代码块完成态正常。
- 1080×700：原文约 564px、AI 398px；根页面宽度等于视口宽度，无横向溢出。
- 设置和媒体库动态加载后均可操作；媒体库只挂载可视行。
- Windows 系统字体栈计算值为 `Segoe UI Variable, Microsoft YaHei UI, Segoe UI, sans-serif`。
- 浏览器截图接口两次超时；DOM 身份、非空状态、计算尺寸、交互、错误遮罩与控制台检查均已完成。

## 备注

- 阶段 5 完成前不升级版本号、不构建安装包。
- 不读取或改写真实用户转写、聊天、媒体库或设置数据。
- 若测量显示某项缓存收益不足或正确性风险较高，将在本文件记录取舍，不为了勾选计划而引入脆弱缓存。
